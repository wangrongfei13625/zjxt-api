spring:
  application:
    name: zjxt-member
  cloud:
    config:
      enabled: true
      uri: http://${http.config.uri}:8888
      profile: ${http.config.profile}

http:
 config:
   uri: 127.0.0.1
   profile: dev

